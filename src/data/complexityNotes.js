// One-paragraph derivations explaining WHY the complexity is what it is.
// Keyed by algorithm slug.

export const COMPLEXITY_NOTES = {
  bubblesort: `外层循环执行 n-1 次，内层循环在第 i 轮执行 n-1-i 次比较，总比较次数 = Σ(n-1-i) for i=0..n-2 = n(n-1)/2 = **O(n²)**。加入"一轮无交换则退出"的优化后，最好情况（已有序）只需一轮 n-1 次比较即 **O(n)**。每次最多发生一次交换，空间仅需一个临时变量 **O(1)**。`,

  selectionsort: `无论输入如何，每轮都必须扫描剩余 n-i 个元素找最小值，总比较次数 = (n-1)+(n-2)+…+1 = n(n-1)/2 = **O(n²)**，且无法提前退出——这是它与冒泡的核心差异。交换次数最多 n-1 次（远少于冒泡），适合写操作昂贵的场景。空间 **O(1)**（原地）。`,

  shellsort: `复杂度由 gap 序列决定。Hibbard 序列（1,3,7,…）最坏 O(n^1.5)，Knuth 序列（1,4,13,…）实验约 O(n^1.3)，Sedgewick 序列理论最坏 O(n^(4/3))。直觉：大 gap 阶段 O(n) 比较但让元素大步移动，后续小 gap 轮每次插入位移很少。精确分析极难，但比 O(n²) 好得多。空间 **O(1)**。`,

  insertionsort: `最坏（逆序）：第 i 轮需要比较并移动 i 次，总计 1+2+…+(n-1) = n(n-1)/2 = **O(n²)**。最好（已有序）：每轮只比较一次不移动，共 n-1 次 = **O(n)**。关键：若数组有 k 个逆序对，运行时间 Θ(n+k)。空间 **O(1)**（原地，移动用覆盖而非交换）。`,

  countingsort: `第一步遍历 n 个元素 **O(n)**；第二步对值域 k 做前缀和 **O(k)**；第三步反向遍历 n 次填充结果 **O(n)**。总计 **O(n+k)**，空间同样 **O(n+k)**（计数数组 k+1，输出数组 n）。当 k = O(n) 时等价于线性排序，可突破比较排序 Ω(n log n) 下界——因为它根本不做元素间比较。`,

  quicksort: `**平均**：每次 partition 的 pivot 期望把数组均分两侧，递推 T(n)=2T(n/2)+O(n) → **O(n log n)**（主定理）。**最坏**：每次 pivot 是最大/最小值，一侧 0 个元素，T(n)=T(n-1)+O(n) = **O(n²)**（有序数组 + 固定末尾 pivot 时触发）。随机化选 pivot 使最坏情况以极低概率发生。递归栈深平均 O(log n)，最坏 O(n)。`,

  mergesort: `把数组分成两半分别排序，再线性时间合并（双指针）。递推式 T(n) = 2T(n/2) + O(n)，用主定理（情形 2）得 **T(n) = O(n log n)**，且与输入无关（无最好/最坏之分）。合并步骤需要临时数组存放结果，空间 **O(n)**。递归栈深 O(log n) 可忽略。`,

  heapsort: `**建堆**：朴素想法 O(n log n)，但自底向上 sift-down 实际是 O(n)——底部节点数多但 sift-down 距离短，精确求和 Σ h·n/2^h 收敛于 2n。**排序**：反复取堆顶（最大值）并 sift-down，n-1 次，每次 O(log n)，共 **O(n log n)**。原地操作，空间 **O(1)**（堆存在原数组）。`,

  bfs: `每个节点最多入队一次出队一次 **O(V)**；出队时遍历该节点所有边 **O(E)**（总计）；故 **O(V+E)**。visited 集合大小 O(V)，队列最大 O(V)，空间 **O(V)**。在稀疏图 E≈V 时近似 O(V)，在稠密图 E≈V² 时近似 O(V²)。`,

  dfs: `同 BFS——每个节点被访问一次 O(V)，每条边被检查一次 O(E)，总 **O(V+E)**。递归实现的隐式栈深度等于 DFS 树的最大深度，最坏（链状图）O(V)；显式栈空间同样 O(V)。`,

  dijkstra: `每个节点最多入优先队列一次（松弛时可重复入队，但每节点最多处理一次），取出最小需 O(log V)，共 V 次 → **O(V log V)**。每条边最多触发一次松弛和入队操作 O(log V)，共 E 次 → **O(E log V)**。合计 **O((V+E) log V)**。朴素数组实现不用堆，取最小 O(V) × V 次 = O(V²)，适合稠密图。`,

  bellmanford: `外层循环 V-1 轮，每轮遍历所有 E 条边做松弛，故 **O(V·E)**。额外第 V 轮检测负环。相比 Dijkstra 慢，但支持负权边。空间 O(V)（dist 数组）。可加"本轮无更新则提前退出"优化，最好情况 O(E)（一轮收敛）。`,

  floydwarshall: `三重循环：枚举中间节点 k (V 次) × 枚举起点 i (V 次) × 枚举终点 j (V 次) = **O(V³)**。dist 矩阵 V×V = **O(V²)**。不可避免的三重循环是瓶颈，只适合 V ≤ 500 左右的图。负权环检测：对角线 dist[i][i] < 0。`,

  toposort: `Kahn 算法：计算入度 O(V+E)；BFS 遍历每个节点出队一次 O(V)，处理每条出边一次 O(E)；总 **O(V+E)**。空间：入度数组 O(V)，队列 O(V)。DFS 后序版本同样 O(V+E)。`,

  prim: `使用二叉堆（优先队列）：每个节点 extract-min 一次 O(log V)，共 V 次；每条边可能触发一次 decrease-key O(log V)，共 E 次。总 **O(E log V)**。稠密图用朴素数组找最小 O(V²) 反而更快（E≈V² 时 O(V² log V) vs O(V²)）。`,

  kruskal: `对 E 条边按权重排序 **O(E log E)**；之后用并查集（路径压缩+按秩合并）检测环，每次操作近似 O(α(V)) ≈ O(1)，E 次共 O(E)。总瓶颈为排序 **O(E log E)**。由于 E < V²，O(E log E) < O(E log V²) = O(2E log V) = O(E log V)，两者等价。`,

  bst: `每次操作沿树高 h 走一条路径。平衡时 h = O(log n) → 查找/插入/删除 **O(log n)**。最坏（按升序插入，树退化为链表）h = n → **O(n)**。中序遍历 O(n)，空间存 n 个节点 **O(n)**。`,

  redblack: `红黑树五条性质保证：黑高（black-height）为 bh，则树高 h ≤ 2bh；又 n ≥ 2^bh - 1，故 bh ≤ log₂(n+1)，即 **h ≤ 2log₂(n+1) = O(log n)**。因此所有操作（查找、插入、删除）最坏 **O(log n)**。Fixup 旋转次数最多 O(1)（插入）或 O(log n)（删除），旋转本身 O(1)。`,

  knapsack: `dp 表有 (n+1)×(W+1) 个状态，每格 O(1) 转移，总 **O(n·W)**（伪多项式，W 是容量值非位数）。空间 O(n·W)；滚动数组优化到 **O(W)**（逆序遍历保证使用上一行值）。注意：W 很大时（如 10^9）需用记忆化搜索或分支限界法。`,

  lcs: `dp[m+1][n+1] 共 (m+1)(n+1) 个状态，每格 O(1) 转移，总 **O(mn)**。空间 O(mn)；可用滚动数组优化到 O(min(m,n))（只需前一行）。回溯还原路径 O(m+n)。`,

  lis: `对每个 i，向左扫所有 j < i，故 T(n) = Σ i = n(n-1)/2 = **O(n²)**。O(n log n) 解法：维护 tails[] 数组（长度 L 的 LIS 末尾最小值），每个 nums[i] 用二分在 tails 中找插入位置，O(log n)，总 **O(n log n)**。基础 DP 空间 **O(n)**（dp + prev）。`,

  nqueens: `严格上界 O(n!)：第一行 n 选择，第二行最多 n-1，…但约束剪枝大幅减少实际搜索量。实际解数约为 O(n!/e^n·√n)（斯特灵）。实际运行远快于 O(n!)，但理论最坏仍是 **O(n!)**。空间：递归栈 O(n)，棋盘状态 O(n)。`,

  fifo: `每个页面引用操作 O(1)（哈希集查找 + 队列操作）。处理 n 个请求共 **O(n)**。队列存最多 capacity 个页面，空间 **O(capacity)**。`,

  lru: `哈希表 O(1) 查找 + 双向链表 O(1) 移动到头部，每次操作 **O(1)**（最坏 O(n) 若实现欠佳）。n 个请求共 **O(n)**。空间 **O(capacity)**。`,

  opt: `每次缺页时扫描剩余访问序列找"下次使用最晚"的页面，最坏 O(n) × capacity 次 = **O(n·capacity)**。或枚举帧 O(capacity)、扫描未来 O(n)，每次缺页 O(n)，共 **O(n²)**（最坏缺页 n 次）。无法在线实现，仅用于离线分析。`,

  diskfcfs: `按到达顺序遍历 n 个请求，每次计算距离 O(1)，总 **O(n)**。空间 **O(n)**（存请求队列）。`,

  sstf: `每次从剩余请求中找最近的，朴素实现 O(n) 扫描，重复 n 次，共 **O(n²)**。用优先队列可优化到 O(n log n)（动态更新最近距离）。`,

  scan: `先对请求排序 **O(n log n)**；之后按方向顺序遍历 O(n)。总瓶颈 **O(n log n)**。空间 O(n)。`,

  naive: `外层循环 n-m+1 次（滑动窗口起点），内层最多比较 m 次，最坏 **O((n-m+1)·m) ≈ O(n·m)**。最好（第一位就不匹配）每次 O(1)，总 **O(n)**。空间 **O(1)**，不需预处理。`,

  kmp: `预处理 failure function：指针 len 单调不减又最多减，总操作 O(m)。匹配：主串指针 i 只增不减 O(n)，模式串指针 j 通过 LPS 回退但 j+回退次数 ≤ i 增量，总 O(n)。合计 **O(n+m)**，空间 **O(m)**（LPS 数组）。`,

  unionfind: `朴素 Union-Find：Find 最坏 O(n)（链状树）。**路径压缩**单独均摊 O(log n)，**按大小合并**单独保证树高 ≤ log n 即 O(log n)。两者同时使用，由 Tarjan 证明均摊代价为 **O(α(n))**（α 为反阿克曼函数，实际 ≤ 4）。n 次操作总代价 **O(n·α(n)) ≈ O(n)**，空间 **O(n)**（parent + size 数组）。`,

  trie: `插入/查找/前缀查询：只沿字符串长度 m 条路径下降，每步 O(1)（哈希子节点），总 **O(m)**，与字典词数 n 无关。空间：最坏每个字符都新建节点，n 个单词总字符数 Σm_i，最坏 **O(n·m_max)**。英文字母表 26 叉实现每节点 26 指针，固定开销较大；哈希子节点节省空间但哈希有常数因子。`,

  linkedlist: `**访问/搜索**：从头遍历，最坏 **O(n)**。**头插/已知位置插入删除**：只改指针，**O(1)**。**尾插**（无尾指针）：需遍历到末尾 O(n)；加尾指针优化后 O(1)。**反转**：一次遍历 **O(n)**，每步仅改 prev/next 两个指针，空间 **O(1)**。对比数组：访问 O(1) vs O(n)；插入/删除 O(n)（移位）vs O(1)（已知位置）。`,

  astar: `时间复杂度取决于启发函数质量：最优启发（h=实际代价）只展开最优路径上的节点 **O(E)**；h=0 退化为 Dijkstra **O((V+E)logV)**。一般情况以分支因子 b、最优解深度 d 表示为 **O(b^d)**。关键：h 越接近实际代价，展开节点越少，越快。空间 O(b^d)（open 集存储）。`,

  hashtable: `平均情况：哈希函数 O(键长)，定位桶 O(1)，链表期望长度 α=n/m（装载因子），查找 **O(1+α)**。保持 α<0.75 视为 **O(1)**。最坏：所有键哈希到同一桶，链表长度 n，退化为 **O(n)**（哈希函数极差时）。**扩容/重哈希**：触发时 O(n) 将所有元素重新散列，分摊到每次插入 O(1)。空间 **O(n)**。`,

  segtree: `**建树 O(n)**：递归构建，每节点算一次和，共 2n-1 个节点，总 O(n)。**区间查询 O(log n)**：每层最多访问 4 个节点（证明：每个端点最多让 2 个区间"碎裂"），总 4·log n 个节点。**单点更新 O(log n)**：从叶到根的路径长度 log n，逐级更新。空间 **O(n)**（4n 数组存树）。懒标记区间更新同样 O(log n)。`,

  // ── 排序（补全） ─────────────────────────────────────────────
  radixsort: `对 d 位数，每位做一轮计数排序：计数排序对 k 个桶（0-9 即 k=10）处理 n 个元素 O(n+k)，重复 d 轮，总 **O(d·(n+k))**。当 d 和 k 为常数（如 32 位整数 d=10、k=10）时退化为 **O(n)**——线性排序。空间 **O(n+k)**（计数数组 + 输出数组）。关键：必须用稳定排序（计数排序）处理每位，否则低位排序会破坏高位已排好的顺序。`,

  bucketsort: `将 n 个元素均匀分入 m 个桶，每桶用插入排序处理。**均匀分布**时每桶期望 n/m 个元素，插入排序期望 O((n/m)²)，m 个桶合计 O(m·(n/m)²) = O(n²/m)；取 m=n 则 O(1)，总 **O(n)**（均匀分布期望）。最坏：所有元素落入一个桶 **O(n²)**（插入排序）。汇聚各桶 O(m+n)。空间 O(n+m)。`,

  // ── 树结构（补全） ─────────────────────────────────────────────
  avl: `AVL 树通过旋转维护"每个节点左右子树高度差≤1"的不变量，由此得高度 h < 1.44·log₂(n+2)——证明：高度 h 的最少节点数 N(h) = N(h-1)+N(h-2)+1（Fibonacci 递推），N(h)~φ^h/√5，即 h = O(log n)。因此**查找/插入/删除全部 O(log n)**（最坏）。插入最多 O(1) 次旋转（至多 2 次），删除最多 O(log n) 次旋转。空间 **O(n)**。`,

  treap: `Treap 是 BST 键 + 堆优先级（随机值）的组合。随机优先级使树的期望高度 = 随机 BST 高度 = **O(log n)**（分析：随机 BST 的期望比较次数 = 2H_n ≈ 2ln n）。插入/查找/删除期望 **O(log n)**，最坏 O(n)（极小概率，优先级极不平衡）。旋转维护堆性质，每次插入最多 O(log n) 次旋转，空间 **O(n)**。`,

  fenwick: `Fenwick 树（树状数组）利用二进制低位技巧：节点 i 负责区间 [i-lowbit(i)+1, i]，lowbit(i) = i & (-i)。**前缀和查询**：从 i 不断减去 lowbit(i) 直到 0，路径长度 = i 的二进制表示中 1 的个数 ≤ log n，故 **O(log n)**。**单点更新**：从 i 不断加 lowbit(i)，同样 ≤ log n 步，**O(log n)**。**建树**：逐一更新 n 次，O(n log n)；或用 O(n) 构建（差分技巧）。空间 **O(n)**（只需原数组等大数组）。`,

  binarysearch: `每次比较将搜索区间缩半：n → n/2 → n/4 → … → 1，需要 **log₂ n** 步。严格递推：T(n) = T(n/2) + O(1)，主定理情形 2 得 **O(log n)**。最好情况（中间元素即目标）**O(1)**。**必要条件**：数组已排序；否则需先排序 O(n log n)，总代价高于线性扫描。空间 **O(1)**（迭代），递归版本递归栈 O(log n)。`,

  // ── 动态规划（补全） ─────────────────────────────────────────
  editdistance: `dp[i][j] 表示将 s1 前 i 字符变为 s2 前 j 字符的最小操作数，共 (m+1)(n+1) 个状态，每格 O(1) 转移（取插入/删除/替换三者最小），总 **O(m·n)**。空间 O(m·n)；滚动数组优化到 **O(min(m,n))**（只需前一行）。注意：Levenshtein 距离满足三角不等式，是真正的度量空间。`,

  coinchange: `dp[i] 表示凑出金额 i 的最少硬币数，从 0 到 amount 每个状态遍历 k 种硬币做转移，共 **O(amount·k)** 次操作。这是伪多项式复杂度（amount 可以很大）。空间 **O(amount)**（一维 dp 数组）。对比贪心：贪心仅在特殊硬币体系（如美元）中正确，一般情况需 DP。`,

  // ── 字符串（补全） ─────────────────────────────────────────────
  rabinkarp: `预处理：计算模式串哈希 O(m)。滑动窗口：利用滚动哈希 hash(s[i+1..i+m]) = (hash(s[i..i+m-1]) - s[i]·base^(m-1)) · base + s[i+m]，每步 O(1) 更新，共 n-m+1 步 **O(n)**。哈希碰撞时做字符级验证 O(m)，期望碰撞极少故期望 **O(n+m)**，最坏（全碰撞）**O(n·m)**。最大优势：可并行查找多个模式串（多项式哈希扩展为 O(n+km)）。`,

  // ── CPU 调度 ─────────────────────────────────────────────────
  cpufcfs: `按到达顺序维护就绪队列，每次调度取队首，每个进程最多入队/出队一次，n 个进程总 **O(n)**。无抢占，适合批处理。主要问题：护航效应（长进程阻塞短进程），平均等待时间可能很大。空间 **O(n)**（队列）。`,

  cpusjf: `每次选择就绪队列中下一 CPU 区间最短的进程。若用排序实现：每次调度 O(n) 或维护有序列表 O(log n)，n 个进程总 **O(n log n)**。可证明 SJF 给出最小平均等待时间（最优性证明：交换论证——若长进程在前，交换后等待时间之和更小）。缺点：需预知 CPU 区间长度（实际通过指数平均预测）。`,

  cpusrtn: `SRTN（Shortest Remaining Time Next）是 SJF 的抢占版。每当新进程到达或当前进程结束，比较所有就绪进程的剩余时间，选最小的运行。优先队列实现：每次事件 O(log n)，n 个进程 + 上下文切换事件共 **O(n log n)**。最优性：SRTN 给出最小平均等待时间（抢占版）。代价：频繁上下文切换开销，且对长进程可能饥饿。`,

  cpurr: `时间片轮转：就绪队列按 FIFO 维护，每个进程运行最多 q 个时间单位。若 n 个进程每个需 1 个时间片，响应时间最坏 O(n·q)。系统开销：每个时间片结束上下文切换 O(1)，n 个进程总调度次数 Σ⌈burst_i/q⌉ ≤ (total_burst)/q + n。**时间片 q 很小→频繁切换 O(n²/q)；q→∞→退化为 FCFS**。空间 **O(n)**（队列）。`,

  // ── 进程同步 ──────────────────────────────────────────────────
  bankers: `银行家算法：安全性检查（Safety algorithm）——遍历 n 个进程，找一个满足 Need ≤ Work 的进程，模拟分配，重复直到所有进程完成，最坏扫描 n² 次（每轮至少找到一个），故 **O(n²·m)**（m 为资源类数，Need 比较 O(m)）。资源请求检查 O(n·m)，之后调用安全性检查 O(n²·m)。空间 **O(n·m)**（Allocation/Need/Available 矩阵）。`,

  philosophers: `哲学家就餐问题是并发与死锁的经典模型，本身不是一个"算法"而是问题实例。常见解法复杂度：**信号量解法** n 位哲学家 n 个叉子，每次进餐涉及 2 次 wait/signal O(1)，n 位总 **O(n)** 每轮。**Chandy/Misra 解法**（消息传递，无中央协调器）：每次叉子转让 O(1) 消息，有限等待时间。**死锁条件分析**：4 个必要条件（互斥/占有等待/不可抢占/循环等待），打破循环等待（奇偶编号法）即可避免死锁。`,

  // ── 数据库 ───────────────────────────────────────────────────
  bplustree: `B+ 树高度 h = ⌈log_(⌈m/2⌉)(n)⌉（m 为阶数，n 为键数），约 **O(log_m n)**（对数底为 m）。**查找 O(log_m n)**：从根到叶，每层二分查找 O(log m) 或顺序扫描 O(m)，总 O(log m · log_m n) = O(log n)。**插入/删除 O(log_m n)**：找到叶节点后可能向上分裂/合并，最坏整路径长度次。**范围查询优势**：叶节点通过链表连接，找到起点后线性扫描 k 个结果 O(log_m n + k)。磁盘 I/O 次数 = 树高，m 很大时 IO 极少（磁盘友好）。`,

  txnIsolation: `事务隔离级别（ANSI SQL）是并发控制协议，非排序算法，无单一"时间复杂度"。核心权衡：**隔离性越高，并发度越低**。两阶段锁（2PL）：获取锁 O(1)，持有直到提交；冲突时等待，吞吐量取决于竞争程度。MVCC（多版本并发控制）：读不加锁（历史版本），写创建新版本，读 **O(1)**，GC 清理旧版本 O(活跃事务数)。**可见性判断**：根据事务 ID 比较 O(1)。各隔离级别可防止的异常：RU 无法防止脏读；RC 防脏读；RR 防不可重复读；Serializable 防幻读。`,

  hashJoin: `Hash Join 分两阶段：**Build 阶段**：将小表 R（|R| 行）散列到 |R| 个桶，每行 O(1) 哈希操作，总 **O(|R|)**。**Probe 阶段**：遍历大表 S（|S| 行），每行哈希后在对应桶中查找匹配，期望桶大小 O(1)，总 **O(|S|)**。整体 **O(|R|+|S|)**——比嵌套循环 O(|R|·|S|) 快得多。空间 **O(|R|)**（哈希表存小表）。Hash 碰撞最坏 O(|R|·|S|)；Grace Hash Join 处理内存不足场景（分区后分批处理）。`,

  // ── 编译原理 ─────────────────────────────────────────────────
  regexNfa: `Thompson 构造法：对正则表达式的每个操作（字符/拼接/选择 |/星号 *）引入 O(1) 个新状态和转移。设正则长度为 m，构造得到的 NFA 最多有 **2m 个状态、3m 条边**（每个操作至多增加 2 状态、3 边），构造过程 **O(m)**，空间 **O(m)**。ε 闭包计算（BFS/DFS）：每次 O(|状态|)。用 NFA 做匹配时每步维护当前状态集，k 个字符 O(k·m)——比回溯正则引擎的最坏 O(k·2^m) 快指数倍。`,

  nfaToDfa: `子集构造（Powerset Construction）：NFA 有 n 个状态，DFA 最多有 **2^n 个状态**（每个子集一个 DFA 状态）；最坏时间 **O(2^n · |Σ|)**（|Σ| 为字母表大小），空间同样 O(2^n)。实践中多数 DFA 状态不可达，工作队列算法（惰性计算）只处理可达子集——平均情况远小于 2^n。ε 闭包计算（BFS）每次 O(n + 边数)。注意：对某些正则（如 (a|b)^n·a·(a|b)^(n-1)）DFA 确实需要指数级状态。`,

  buildAst: `递归下降解析器：对长度 m 的 token 流，每个 token 被消耗一次，每个递归调用处理常数个 token，故 **O(m)**。构建的 AST 节点数 ≤ m（每个操作数/运算符对应至多一个节点），空间 **O(m)**。比较：LR 解析（移入-归约）O(m) 但需预先生成解析表 O(|文法|³)；LL(1) 表驱动同样 O(m)。**递归下降优势**：直观、易写错误恢复，但左递归文法需改写（如 expr→expr+term 改为 expr→term expr'）。`,

  // ── 计算机网络 ───────────────────────────────────────────────
  tcphandshake: `TCP 三次握手是一个固定 3 步的协议（SYN→SYN-ACK→ACK），状态机转换 **O(1)**，与数据量无关。往返时延（RTT）1 次 = 1.5 RTT 建立连接。四次挥手同样 O(1)（FIN→ACK→FIN→ACK）。TIME_WAIT 等待 2·MSL 是为确保最后 ACK 到达，是时间代价非计算代价。连接建立的实际瓶颈是网络延迟（RTT），不是计算复杂度。`,

  tcpcongestion: `TCP 拥塞控制（Reno/CUBIC）包含四个阶段，均为状态机 O(1) 更新。**慢开始**：cwnd 每 RTT 翻倍，从 1 增长到 ssthresh，需 log₂(ssthresh) 个 RTT。**拥塞避免**：cwnd 每 RTT 加 1（线性），从 ssthresh 增到丢包点，需 O(BDP) 个 RTT（BDP = 带宽·延迟积）。**快重传/恢复**：检测到 3 个重复 ACK，cwnd 减半，O(1)。CUBIC 用三次函数代替线性，同样 O(1) per RTT 更新，但收敛更快。整体是**实时响应协议**，无批处理复杂度。`,

  slidingwindow: `滑动窗口协议：发送方同时可发送 W 帧（窗口大小），接收方缓存乱序帧。**吞吐量**：理论最大 W/(1+2a) 其中 a = 传播延迟/发送时间。计算开销：每帧到达/确认时更新窗口边界 **O(1)**；序列号比较 O(1)；缓冲区插入（有序）最坏 O(W)（选择性拒绝需找插入位置）。处理 n 帧总 **O(n)**（每帧常数操作）。窗口大小 W 是性能参数，不影响计算复杂度。`,

  // ── 信息安全 ─────────────────────────────────────────────────
  rsa: `**密钥生成**：生成两个 k/2 位大素数（Miller-Rabin 测试，O(k²·log k) 每次，期望 O(k) 次尝试），计算 n=p·q、φ(n)、e（互质）、d=e⁻¹ mod φ(n)（扩展欧几里得 O(k²)）。**加密/解密**：模幂运算 c = m^e mod n，用快速幂 **O(k²)** 次位运算（k 为模数位数，即密钥长度）。**安全性基础**：分解 n=p·q 的最佳算法（GNFS）复杂度约 O(e^(c·n^(1/3)·(log n)^(2/3)))——次指数但非多项式。2048 位密钥 k=2048 时，加解密约 O(2048²) ≈ 4×10⁶ 位运算。`,

  dh: `**Diffie-Hellman 密钥交换**：双方各选随机私钥 a, b（O(k) 位），计算 g^a mod p、g^b mod p 并交换，各自计算 (g^b)^a mod p = g^(ab) mod p。**每次模幂** g^x mod p 用快速幂 **O(k²)** 次位运算（k = 模数位数 ≈ 2048 位）。安全性基于**离散对数**难题：已知 g, p, g^a mod p 反推 a，最好算法（GNFS）同 RSA 量级。**ECDH**（椭圆曲线版本）：同等安全性仅需 256 位密钥，模幂变为点乘，O(k²) 但 k 小得多，性能大幅提升。`,

  // ── 计算机组成 ───────────────────────────────────────────────
  ieee754: `IEEE 754 单精度（32 位）编解码：符号 1 位 + 阶码 8 位 + 尾数 23 位。**编码**（十进制→二进制）：整数部分逐位除 2 O(log n)，小数部分逐位乘 2 O(精度位数)，规格化 O(1)，总 **O(log n)**（n 为数值大小）。**加减法**：对阶（右移小阶数尾数）O(k) + 尾数加减 O(k) + 规格化 O(k) = **O(k)**（k=23 为常数）→ 实际 **O(1)**。**乘除法**：尾数乘除 O(k²) 或用 FMA 电路 O(1)。特殊值（±0, ±∞, NaN）均 O(1) 判断。`,

  cachemapdirect: `直接映射 Cache：地址 → 组号 = (地址 / 块大小) mod 组数，单次模运算 **O(1)**（硬件实现为位提取）。访问：比较标记位 O(1)，命中/缺失 O(1)。**缺页处理**：替换该组唯一位置（无需选择算法），O(1)。性能分析：命中率取决于访问模式，冲突缺失（两地址映射同一组）是主要问题。空间开销：Cache 大小 S，每块额外 tag + valid 位 O(log(主存大小/S))。`,

  cachemapset: `组相联映射：地址 → 组号 O(1)（位提取），组内 k 路比较需 **O(k)** 次标记比较（实际并行，硬件 O(1)）。替换算法：LRU O(1)（维护位）；随机 O(1)；FIFO O(1)。**总访问时间 O(1)**（k 为常数，通常 2/4/8 路）。组相联是直接映射（k=1）和全相联（k=组大小）的折中：命中率高于直接映射，硬件代价低于全相联。`,

  cachemapfully: `全相联映射：每个主存块可映射到 Cache 任意位置。访问时需**比较所有 C 个 Cache 块的标记**：串行 O(C)，并行（CAM 内容寻址存储器）**O(1)**，但 CAM 硬件极贵，只在 TLB（Translation Lookaside Buffer）等小容量场合使用。替换用 LRU、Clock 等：LRU 维护链表 O(1)（双向链表 + 哈希）。优点：冲突缺失为零；缺点：硬件复杂度与 Cache 大小线性相关。`,

  pipelineideal: `理想流水线：k 级流水线，每级耗时 Δt，处理 n 条指令时间 = (k + n - 1)·Δt。加速比 = 非流水 n·k·Δt / 流水 (k+n-1)·Δt → **k**（当 n >> k 时）。n 大时线性加速，n=1 无加速（k 个时钟周期对 1 条指令）。时间复杂度：处理 n 指令 **O(n)**（每条指令穿越流水线常数时间）；吞吐量极限 1/Δt（每时钟周期一条）。`,

  pipelinehazard: `流水线冒险（Hazard）是额外的停顿（stall）开销：**数据冒险**（RAW）：无前递时需等待 d 个周期（d = 依赖距离），前递（Forwarding）消除大多数数据冒险，CPI 接近 1。**控制冒险**（分支）：分支预测错误惩罚 p 个周期（p = 分支解析级数），动态预测（2 位饱和计数器）准确率约 90%+，有效 CPI ≈ 1 + miss_rate·p。**结构冒险**：资源冲突（如单端口存储器），需增加端口或延迟。综合 CPI ≈ 1 + hazard_stalls_per_instruction，分析 n 条指令运行时间 **O(n)**。`,
}
